import { finishRideController } from './controller'
import { finishRideValidator } from './validator'

export const finishRide = {
  controller: finishRideController,
  validator: finishRideValidator,
}
